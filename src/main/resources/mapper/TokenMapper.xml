<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.item.study.mapper.TokenMapper">

    <!--    &lt; for < (less-than)-->
    <!--    &gt; for > (greater-than)-->
    <!--    &amp; for & (ampersand)-->
    <!--    &quot; for " (double quote)-->
    <!--    &apos; for ' (single quote)-->

    <cache/>

    <insert id="insert">
        INSERT INTO s_token (userId, value) VALUES (#{id},#{value});
    </insert>

    <update id="update">
        UPDATE s_token
        <set>
            value=#{value}
        </set>
        WHERE userId=#{id};
    </update>

    <!--  添加缓存  -->
    <select id="query" resultType="com.item.study.model.entity.TokenEntity">
        SELECT * FROM s_token where userId=#{id};
    </select>

    <delete id="delete">
        DELETE FROM s_token WHERE userId=#{id};
    </delete>

    <update id="resets">
        UPDATE s_token
        <set>
            value=NULL
        </set>
        WHERE value IS NOT NULL AND time &lt; NOW();
    </update>

    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM s_token WHERE userId=#{id};
    </select>

</mapper>